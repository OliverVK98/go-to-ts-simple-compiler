function test(b: string, a: string, c: number[]): string {
	let newString: string = a + 20;
	function test(b: number, a: number, c: number[]): string {
	let newString: number = a + 20;
	function test(b: boolean, a: boolean, c: number[]): string {
	let newString: boolean = a + 20;
}
}
	return newString;
}
